# Tennis-Tracker-Hub

<!-- redeploy trigger: 2026-07-06, working around a stuck GitHub Pages deployment queue -->

## Project Overview
Monorepo combining the Garmin tennis tracker app (MatchMind) and the Training Hub web dashboard, connected via Supabase. The goal is to keep developing both apps independently while integrating them through Supabase: the Garmin app records match data on-watch and syncs it to Supabase; the Training Hub web app reads that data and displays tennis stats alongside all other training data.

## Apps

### Training Hub (`apps/training-hub/`)
- Single-file HTML/JS/CSS web app (~7000+ lines in `index.html`)
- Deployed via GitHub Pages
- Multi-sport training dashboard: Gym · Climbing · Rehab · Planner · Tennis
- **Tennis tab LIVE (2026-06-12):** end-to-end pipeline verified — watch records match → syncs to Supabase `matches` → Tennis tab displays history, win rate, recent form, per-match stats
- Main file: `index.html`
- Live at: https://fpv64ncs5p-maker.github.io/Tennis-Tracker-Hub/

### Garmin Tennis Tracker (`apps/garmin/`)
- Monkey C app for Garmin watches (tested on Vivoactive 6)
- Branded as **MatchMind** (app files named TennisTracker)
- Tracks live tennis matches on-watch, syncs to Supabase after each match
- Key files: `source/TennisMatchEngine.mc`, `source/SupabaseSync.mc`
- Store: https://apps.garmin.com/apps/a4302e08-340f-4a11-8970-1cb44e7ab34f

## Tech Stack
- **Garmin app:** Monkey C (ConnectIQ SDK 9.1.0, API 6.0)
- **Web app:** HTML, JavaScript, CSS (single file, no build step, no external CDN)
- **Backend/Database:** Supabase — Training Hub project (`pmzzmvzbgeonjnbfreze`), `matches` table
- **Deployment:** GitHub Pages (web), ConnectIQ Store (Garmin)
- **Version control:** GitHub (private repo)

## Key Principles
- Always ask before making structural changes
- Keep this CLAUDE.md updated with important decisions
- Garmin app and web app are deployed independently
- Supabase is the bridge between both apps — Garmin writes, Training Hub reads
- Training Hub is mobile-first (700px max width), offline-first, light mode only
- MatchMind MVP principle: "I can play a full set and log every point without frustration"

## Security
- `Secrets.mc` is excluded from git (listed in `.gitignore`) — contains API keys and credentials for the Garmin app
- The GitHub repo is private
- Supabase RLS: anon INSERT (watch app) + anon SELECT (web app) + anon DELETE (web "Delete permanently"). The anon key is public — accepted trade-off for a personal tracker (`db/2026-06-16_anon_delete_matches.sql`)

## Current Version
- **v1.5.1 (built + submitted to the store 2026-07-28, awaiting approval): retried uploads got the wrong date.** Jo's 27/07 training match failed to sync on court, sat in the retry queue, and uploaded on next app open (28/07 10:35) — but `SupabaseSync.buildPayload()` builds `match_date` with `formatTimestampUtc()` = *now*, so the row landed dated 28/07. The queued payload never carried the match time. Fix (3 files): `MatchPersistence.queueSupabasePayload()` stamps `mDate` (ISO UTC, match-end moment) onto the payload + new `nowUtcIso()`/`pad2()`/`slotDate()` helpers; `SupabaseSync` gained `_dateOverride` + `setDate()`, `buildPayload()` uses it when set; `App.retryNextPending()` passes `MatchPersistence.slotDate(state)`. Payloads queued by v1.5.0 and earlier have no `mDate` → `null` → falls back to "now" (unchanged behaviour). Duration was NOT affected (rebuilt from stored `elapsed`, the 40-min value came through correctly). **Data repair:** row 28 (dated 28/07) was deleted and re-inserted as row 29 dated `2026-07-27T17:00:00Z` (19:00 local) via anon INSERT+DELETE from the browser — Supabase has no anon UPDATE policy. Time was Jo's estimate ("evening ~19:00"), the watch history screen shows only the date. **v1.5.0 confirmed live on the watch** (serve/return columns populated on this match).
- Garmin app: **v1.4.9** (approved + real-watch tested 2026-06-15 — tap-scoring fix confirmed working). v1.5.0 (serve/return points-played) submitted 2026-06-15, awaiting approval. v1.4.8 approved + real-watch tested 2026-06-15: Undo button works, but surfaced a tap-scoring bug (ERROR taps near the top of the button often registered as YOU/WON). v1.4.7 approved + real-watch verified 2026-06-12: sync works, rows arrive in `matches`.
- v1.4.9 (tap-scoring fix — Option 2 "buffer + bigger buttons"; `MainView.mc` only): ERROR/D.FAULT sat in a thin strip directly under the huge "tap top half = YOU won" zone with NO gap, so an ERROR tap landing a few px high silently scored for the player — made worse by v1.4.8's UNDO bar eating the bottom of the scoring band. Fix: a NEUTRAL DEAD-ZONE between WON and the buttons — a tap in `[deadZoneTopY 58%, buttonTopY 63%)` does nothing (grey blink). Buttons enlarged (now `63%→87%`, was `66%→86%`), score block nudged up (oval bottom now 58%, clears the buffer), UNDO slimmed to a 32%-wide bar. Tap zones top→bottom: WON `y<58%` · DEAD `58–63%` · ERROR/D.FAULT `63–87%` (split at centre x) · UNDO `y>87%`. iCloud build source edited; `apps/garmin/source/` mirror synced.
- v1.4.8 (4 fixes from first real-watch evening with working sync):
  1. **Supabase retry QUEUE** (5 slots, `supabase_q_0..4`, legacy key migrated) — single slot meant a failed upload was OVERWRITTEN by the next match (one match permanently lost 2026-06-12). Queue drains by chaining: each successful `onResponse` triggers `retryNextPending()`.
  2. **MatchMenu tap-twice confirm** — first tap highlights, second tap executes (RESUME pre-highlighted, so single-tap resume still works). Fixes mid-match SAVE/LATER/DISCARD mis-hits.
  3. **LATER stops + saves the recording** (`stopSessionForLater`, no sync) — previously left the session running, watch OS showed its own save dialog, matches "saved" there bypassed sync + history. One interrupted match = two Garmin activities (Jo's choice over losing HR data).
  4. **Dedicated UNDO button** (replaces MatchMind branding strip below ERROR/D.FAULT) — swipe-up undo registers as a top-half TAP on the real watch, ADDING a point instead of undoing. Swipe-up still works as fallback.
- v1.4.6: approved + installed on watch 2026-06-12. Real-watch test: activity saves to Garmin Connect but NO request reaches Supabase (confirmed via API Gateway logs). curl test with same key/payload → 201, so Supabase side is fully working; failure is on-watch and silent.
- v1.4.1: bigger fonts in MatchMenu and MatchHistory
- v1.4.2: readability improvements across Setup, MainView, MatchMenu, MatchHistory
- v1.4.3: fix Supabase sync — correct anon key in Secrets.mc; Supabase ALTER TABLE added DEFAULT 0 to stat columns, dropped NOT NULL from hr_avg/hr_max/player_served_first
- v1.4.4: remove set_scores array-of-dicts from payload — CIQ JSON serializer silently failed on nested arrays, preventing makeWebRequest from firing at all
- v1.4.5: remove hr_avg/hr_max null values + convert playerServing Boolean to 1/0 — CIQ JSON serializer silently fails on null and Boolean types
- v1.4.6: rebuild + resubmit of the v1.4.5 payload fixes. NOTE (2026-06-12 correction): the versionCode theory was wrong — the sed edit landed in the monorepo manifest (never built), the submitted .iq had NO versionCode, and the store update still reached the watch fine. versionCode is not part of the CIQ manifest schema and is irrelevant. See `MatchMind_Supabase_Sync_Findings.md`
- v1.4.7: **ROOT CAUSE FOUND & FIXED** — `"Content-Type" => "application/json"` (string) fails CIQ's local header validation with -200 (INVALID_HTTP_HEADER_FIELDS_IN_REQUEST); the request NEVER left the watch, on simulator and real device alike, in every version since sync was built. Fix: `"Content-Type" => Comm.REQUEST_CONTENT_TYPE_JSON` (enum constant). Verified in simulator: SYNC OK + rows inserted in `matches` (incl. retried stuck payload). Also in v1.4.7: visible sync status (new `SyncStatus.mc`) in PostMatchView + SetupView — `SYNC ...` / `SYNC SENT` / `SYNC OK` (green) / `SYNC ERR n` (red) / `SYNC EXC`; and Prefer header → `return=representation` (empty 201 body from `return=minimal` triggers CIQ -400 with JSON responseType, disguising success as failure)

## Deployment

### Training Hub (web app)
- Edit `index.html` in `apps/training-hub/`
- Copy changes to root: `cp apps/training-hub/index.html .` (the root file is what GitHub Pages serves)
- **Deploy via GitHub Desktop** (replaces the old `curl`/GitHub-API push): open GitHub Desktop → review the diff → type a summary → **Commit to main** → **Push origin**. GitHub Pages auto-deploys in ~1 min. Desktop handles auth, so no token needed.
- GitHub Desktop and Claude both work in the **same** folder (`~/Documents/Tennis-Tracker-Hub`) — single source of truth, no duplicate clones.
- Day-to-day cheat-sheet: see `GIT_WORKFLOW.md` (repo root).
- Live at: https://fpv64ncs5p-maker.github.io/Tennis-Tracker-Hub/

### Garmin App
- ⚠️ **The build source of truth is the iCloud folder** (`PROJ007_Garmin App/Tennistracker/`) — `package.sh`/`run.sh` build THERE. `apps/garmin/` in this repo is a mirror for version control. Always edit the iCloud copy (or edit both), then keep them in sync — on 2026-06-12 a fix applied only to the mirror never made it into the built `.iq`.
- Source code in `apps/garmin/source/`
- `Secrets.mc` is NOT in git (excluded via .gitignore) — kept locally only
- Build for simulator: `./run.sh` (from iCloud project folder)
- Build for store:
  ```bash
  cd ~/Library/Mobile\ Documents/com~apple~CloudDocs/01\ Claude\ in\ Docs/02\ Projects/PROJ007_Garmin\ App/Tennistracker
  ./package.sh
  ```
  → generates `bin/Tennistracker.iq`
- Submit `.iq` file to: https://developer.garmin.com/connect-iq/sdk/
- Garmin approval takes ~2 hours
- Users install update from ConnectIQ Store

## Supabase Integration
- Project ID: `pmzzmvzbgeonjnbfreze`
- Table: `matches` (29 columns: all engine stats incl. serve/return points played+won, + nullable opponent_name, location, notes)
- RLS: anon INSERT (watch) + anon SELECT (web) + anon DELETE (web permanent-delete, `db/2026-06-16_anon_delete_matches.sql`)
- Watch app credentials in `Secrets.mc` (gitignored)

## Training Hub — Key Context
- **Architecture:** single-file, localStorage-first, no build step
- **Sync:** Supabase `user_data` table (migrated from GitHub Gist 2026-05-27 — DNS issue no longer present, Supabase confirmed working on mobile)
- **Tennis integration:** Tennis tab reads `matches` from Supabase (REST + anon key) and merges them with manually-logged matches (`tennis_matches` in localStorage).
- **Watch-match editing (overlay):** watch rows are not editable in the DB (no anon UPDATE). Web-side enrichment — Competition/Training category, partner, opponent, location, notes — and "remove from tab" (soft-hide, reversible) live in a `tennis_overlay` localStorage object keyed by match id, synced across devices via `user_data`. Two removal modes: "remove from tab" = reversible soft-hide (overlay only, no DB change); "Delete permanently" removes the row via an anon DELETE policy (`db/2026-06-16_anon_delete_matches.sql`). Doubles get a 2nd opponent field (Partner + Opponent 2 shown only for doubles). (Live + confirmed by Jo 2026-06-15.)
- **Serve/return %:** needs `service_points_played` + `return_points_played` columns (`db/2026-06-15_add_serve_return_played.sql`) + MatchMind **v1.5.0** to send them (engine already counts them; web already reads them, NULL-tolerant). v1.4.9 approved 2026-06-15; v1.5.0 submitted 2026-06-15, awaiting approval. **SQL columns added 2026-06-15 (confirmed by Jo, before submission)** — once v1.5.0 is approved + installed, serve/return won-lost + win% populate for new matches.
- **Unfinished matches (2026-07-15):** the watch sends `result:"abandoned"` for matches stopped before completion (e.g. SAVE at 5-4 with no set finished); the web previously defaulted missing/other results to `'lost'`, showing a false LOSS badge. Now: grey **UNFINISHED** badge (history card, detail modal, watch-edit summary; grey left border), and abandoned matches are EXCLUDED from win rate, W/L counts, recent-form dots, and the Home month-comparison win% (`fin` counter) — they still count in total matches (Stats panel shows an "Unfinished" row). All stats remain from Jo's perspective (games 5/4 = Jo won 5). Jo chose the neutral badge over inferring a result from the games lead (2026-07-15).
- **Per-point error/DF rates (2026-07-15):** error/DF stats switched from per-match averages to length-independent rates — Stats panel "AVERAGES" card shows **unforced errors / 100 points played** (points = serve+return played, fallback points won+lost) and **DFs / 100 serve points**, with the old per-match avg as sub-text; Home month-comparison "Errors / 100 pts" likewise (falls back to per-match "Avg errors" when no point counts exist, e.g. pre-v1.5.0 matches). Rates are aggregate (total errors ÷ total points), not average-of-match-rates. Rationale: fair comparison across 1-set/Bo3/unfinished matches; unfinished matches contribute to all rate stats, only win/loss excludes them.
- **Rehab tab rebuilt as dated session log (2026-07-16):** Jo restarted the full Achilles rehab program (round 2). Old model (3 bubbles + 1 RPE per whole phase, no per-exercise done, no real weekly count) replaced by v2: `rehab_data` migrated to `{_v:2, archive, startDate, phaseStarts, weights, sessions:[]}` — the entire 2025 round auto-archives on first render into `data.archive`, shown read-only in a collapsed "📦 Previous program" card (per-phase sessions done, RPE, weights). New per-phase **"➕ Log session"**: dated entry with per-exercise done dots, per-session weights (prefill: last session → phase default → protocol default; comma decimals OK via `_normalizeKgInput`), RPE; sessions listed under each phase with ✏️ edit and 🗑 delete (confirm + 5s undo). **"This Week" card** counts sessions across all phases per Monday-based week (dots + count + last-week comparison) — this is the real 2–3×/week tracking. Phase headers show a session-count chip. Phase-level default weights table and phase start-date overrides unchanged. `REHAB_PHASES` protocol unchanged (same FisioHolland 5 phases, Jo confirmed). Old `toggleRehabSession`/`saveRehabRPE` removed. Tested via jsdom (migration, log, edit, delete, undo, weekly count all pass). **Gym-test fix (2026-07-17):** weight inputs were too narrow (value overflowed into the Intensity column) and typing appended to the old value — grid now `1fr 30px 30px 96px 44px` ("Int" header), inputs bigger/bold 15px with `onfocus="this.select()"` (tap = whole value selected, typing replaces), phase-table input switched `type=number`→`text inputmode=decimal` + `_normalizeKgInput` (EU comma decimals). **Draft persistence fix (2026-07-17):** the open log form lived only in memory, so switching to the Gym tab (mid-workout) or a page reload wiped ticks/weights/RPE — same failure mode as the 2026-06-22 climb-draft loss. Now the in-progress form is a draft in localStorage key `_rehab_log_draft` (NOT in `DATA_KEYS`, no cloud sync): every tick (`toggleRehabExDone`), typed weight (`_rehabLogW` oninput), RPE/date (`_rehabLogField`) saves it; `renderRehab` silently restores it and re-opens the phase; save/cancel clear it. Inputs render from draft state (fallback: prefill chain). jsdom-tested: tab switch, full reload, save, cancel. **Home week calendar shows logged rehab (2026-07-17):** `renderHomeWeekCalendar` overlays 🦵 on days with a logged rehab v2 session (reads `rehab_data.sessions` directly — safe on unmigrated data, no forced migration on Home). Deduped against a *planned* rehab icon (same emoji, one shown). Calendar now also renders the 7-day row when no planner week exists (previously blank; days built via `addDays(monday,i)`). Rehab stays excluded from the Active-days count (decision: active = Gym/Climb/Tennis/Golf only). Follow-up: `.week-icons` had a FIXED 28px height, so days with 2+ icons overflowed onto the Active-days bar (why Jo missed the 🦵) — now `min-height:28px` + wrap, icons 14px. Edited in `apps/training-hub/index.html` + copied to root.
- **Home Tennis Momentum empty on first load (2026-07-28):** `getTennisTrends()` reads the `_sbMatches` cache, but `_fetchSbMatches()` was only ever called from `renderTennis()` — so on a fresh page load Home rendered before any watch match existed in memory and showed "No tennis data yet" until the Tennis tab had been opened once (then Home was correct, which made it look intermittent). Fix: `_fetchSbMatches()` is now also fired at `DOMContentLoaded` (fire-and-forget, after `renderHome()`), calls `renderHome()` as well as `renderTennis()` when the rows land, and is protected by a new `_sbFetching` in-flight guard so the startup call and a fast Tennis-tab open can't double-request. Still silently no-ops offline (unchanged). jsdom-tested: one fetch fired, Home shows the match without visiting the Tennis tab. Edited in `apps/training-hub/index.html` + copied to root.
- **Competition/Training filter (2026-06-23):** Tennis tab has a second segment row (`#tennis-cat-filter`: All · Competition · Training) below the History/Stats toggle. `_tennisCatFilter` + `tennisCatFilter()` filter the single `matches` list in `renderTennis` via `catOk`, so summary cards, recent-form dots, Stats panel AND History list all recalculate for the chosen category — Competition and Training stats never mix. Uncategorised matches (no `category`) only appear under "All".
- **Weight suggestions: latest data wins (2026-07-17):** Jo noticed in-session weight adjustments weren't reflected in the next session's suggestions. Root cause: `suggestWeightFull` checked `gym_orm_cache` (best-EVER Epley 1RM, ratchets up only) first and returned immediately — latest assessment and recent sessions were shadowed; down-adjustments never propagated. Rewritten: suggestion now uses the most RECENT data point — latest session entry with weight+reps (compared by session date, not array order, so back-dated entries can't win) vs latest assessment containing the exercise; newer date wins, tie → session. Best-ever 1RM still cached on save (`_updateORMCache` unchanged, still prefills assessment forms) but only appears in the hint label as "· PR 1RM x kg (date)". `source:'cache'` no longer exists (callers only branched on 'assessment' — verified safe). jsdom-tested: down-adjust followed, newer assessment wins, ref fallback, back-dated session ignored.
- **Number legibility bump (2026-07-17):** Jo found numbers (weights/reps/sets) hard to read at the gym. App-wide CSS rule `input[type=number],input[inputmode=decimal],input[inputmode=numeric]{font-size:16px!important;font-weight:700!important}` — one rule covers all ~99 numeric inputs (`!important` needed to beat scattered inline 13px styles); 16px also stops iOS auto-zoom on input focus. Displayed numbers: `.ex-row-meta` 12→14px semibold (gym log/history sets×reps×kg lines), `.meta-chip` 11→13px, `.rehab-ex-row`/`.rehab-log-row` 13→14px. Labels/layout untouched ("bigger numbers only" — Jo's choice over raising base font, which risked breaking tight grids). Time inputs (inputmode=numeric, full-width) also get 16px bold — acceptable. FB tab number chips use inline styles and weren't bumped — revisit if Jo flags them.
- **Tennis tab reorganised into a hub (2026-07-29):** the Tennis tab now lands on a **hub** with two cards — **📋 My tennis matches** (the entire previous screen: History/Stats toggle, Competition/Training filter, summary cards, recent form, match list — unchanged) and **⏱️ Score a match** (new). `switchTab('tennis')` calls `tennisGo('hub')` instead of `renderTennis()`; `tennisGo(view)` toggles `#tennis-hub` / `#tennis-matches-view` / `#tennis-score-view` and each sub-view has a `← Tennis` back link. Jo chose "always open the hub" over remembering the last screen (predictable, costs one tap). Hub cards show live badges: match count, and `● MATCH IN PROGRESS` / `n saved` for scoring.
- **Score a match — scoresheets for other people (2026-07-29):** Jo scored a friend's match on her watch and it polluted her own data. New module under the Tennis hub: score someone else's match on the phone, export a PDF to share, then delete it.
  - **Engine:** `tsCreate/tsInput/tsAward/tsWinGame/tsCheckSet/tsCheckTB/tsCheckSuperTB/tsUndo/tsScoreLine` — a JS port of the watch's `TennisMatchEngine.mc`, generalised to **two sides** (the watch only ever scores from Jo's perspective). Same rules: 0/15/30/40/Ad, deuce, tiebreak at 6-6 (first to 7 by 2), super-TB decider (first to 10 by 2, entered *before* the deciding set starts), serve rotation (every game; in a tiebreak 1 point then every 2), changeover on odd games / every 6 TB points, and full stats (winners, unforced errors, DFs, serve/return points played+won, tiebreaks won). State is a plain serialisable object; the undo stack is JSON snapshots (max 200) that deliberately **exclude the timer fields** so an undo doesn't rewind the clock. Formats: 1 Set · Best of 3 · Best of 3 + Super TB · Super TB (10) · Tiebreak (7).
  - **UI:** setup (names, singles/doubles, format, serves first, event) → live screen with a per-player column (**WON** / **ERR** / **D.F**), score box (sets · games · points · clock · SWITCH ENDS banner), and a full-width UNDO bar. The non-server's **D.F** button is disabled — a receiver can't double fault, so that whole class of mis-tap is gone (the watch's own 2026-06-15 tap-scoring lesson). Clock ticks only while the screen is open, and is editable in the Finish dialog, so a sleeping phone can't inflate the duration.
  - **Draft safety:** every single point writes the full match state to `_tennis_score_draft`, plus `visibilitychange`/`pagehide` saves — same protection as the climb draft (2026-06-22). A resume banner appears on the scoring home screen and a badge on the hub card.
  - **Storage isolation (the important bit):** `tennis_scoresheets` and `_tennis_score_draft` are written with **raw `localStorage`, never `DB.set()`**, and are **NOT in `DATA_KEYS`** — so they never sync to Supabase, never appear in an export/backup, and never touch win rate, recent form, or the Stats panel. Same reasoning as abandoned matches (2026-07-15) and the parked Garmin TRAINING mode (`drills` table, not `matches`).
  - **PDF via print, no library:** `tsExport()` renders a report into `#print-root` (a direct child of `<body>`, `display:none` on screen) and calls `window.print()`; a `@media print` block hides `body > *` except `#print-root`. On iPhone: print sheet → Share → save to Files / WhatsApp / Mail. Chosen over bundling jsPDF (~350KB) to keep the single-file, no-CDN, no-build architecture. `afterprint` clears the report, with a 120s fallback because iOS Safari doesn't always fire it.
  - **Bug found on the way:** `showToast()` was called in three places in the tennis code but never defined (the real helper is `toast()`), so those calls silently threw. Added a `showToast` alias rather than renaming the call sites.
  - Headless-tested (81 assertions, jsdom): deuce/advantage, both sides' inputs, 6-0 set, 6-6 tiebreak stored as 7-6(n), super-TB entry at 1-1 and the 2-clear rule, serve/return counters, TB serve rotation, undo (incl. not rewinding the clock), hub navigation, full tap-through match, draft survives a page reload, save/delete, XSS escaping of player names, PDF contents, and that a scoresheet never reaches `DATA_KEYS`, `tennis_matches` or Jo's history.
- **Tabs:** Home · Gym · Climbing · Rehab · Planner · Tennis (hub → My matches / Score a match)
- **Climb draft recovery hardened (2026-06-22):** Jo lost an in-progress bouldering session when the phone screen turned off. Root cause: the climb draft autosave only fired *after* a climb was logged and `_restoreDraftClimb` discarded any draft with zero logged climbs — so a "session" that was just gym + meters was never saved. Fixes (Climbing tab, `index.html`): (1) `_saveDraftClimb` now saves on any started session — gym/date/duration/custom-wall changes too, not just logged climbs — and skips while editing a saved session (`_editingClimbId`); stores `customWall`. (2) New `visibilitychange`/`pagehide` listeners save the draft when the app is backgrounded / screen sleeps (iOS-reliable). (3) `_restoreDraftClimb` offers the Resume banner for any in-progress session, not only when climbs are logged. `restoreClimbDraft` restores custom wall height + reveals the field.
- **Add-your-own-gym + multi-wall (2026-06-22):** gym dropdown is rendered from `GYMS` (base) + a new persisted `custom_gyms` key (in `DATA_KEYS`, so it cloud-syncs). "➕ Add new gym…" opens `#modal-add-gym` (name + "has bouldering" checkbox + comma-separated rope-wall heights → `saveNewGym`). A gym value is EITHER a plain number (legacy/base form) OR `{walls:[14,18],boulder:true}`; accessors `gymWalls`/`gymHasBoulder`/`gymPrimaryHeight`/`gymLabel` normalise both. `getWallHeight`→`gymPrimaryHeight`. Each routes/Boulder block's wall-height picker is built by `gymWallOptions(b.wallHeight)` from the selected gym's walls (changing the gym re-renders blocks). Base `GYMS` stays numeric — several history meter calcs do `GYMS[s.gym]||s.wallHeight||0`; custom gyms fall back to the per-session `wallHeight` saved at log time.
- **Boulder vs Routes surfaced (2026-06-22):** dedicated "🪨 Boulder" block button next to "🧗 Routes" (creates a routes block preset to `climbingType='Boulder'`); routes-block header shows Boulder vs Routes. The Lead/Top-Rope/Boulder dropdown was previously buried inside a block.
- **Inline "Add another block" (2026-06-22):** the Routes/Boulder/Board/ARC add-block buttons now also render inline *between the blocks and Cool-down* (built in `renderClimbBlocks`), so adding the next burn needs no scrolling. The static bottom row (`#add-block-row`) is hidden once a session has content (`hasData`) and only shows to start the first block. Each block = one burn; block count + routes-per-block are intentionally meaningful. Remove via the block's ✕ (`removeClimbBlock`).
- **History sorted by date (2026-06-22):** Climbing, Gym, and Climbing Gym (FB) history lists now sort by session `date` descending (tie-break: most recently added `id`), so back-dated entries land in the right spot instead of at the top (`renderClimbHistory`/`renderGymHistory`/`renderFBHistory`). Gym already sorted; its sort + the others made null-date-safe.
- **Climb Started/Finished times (2026-06-22):** replaced the manual "Duration (min)" field with **Started** + **Finished** time inputs (`climb-start`/`climb-end`, `autoColonTime` + `updateClimbDuration`), mirroring the Gym tab. Total is auto-computed and shown in `#climb-duration-display`. Saved sessions now store `startTime`/`endTime` and the computed minutes in the existing `duration` field (so history, Week Totals, and Home — which read `duration` — keep working). Draft autosave stores `start`/`end`; `editClimbSession` repopulates the times.
- **Core exercise library expanded (2026-07-02):** added 13 exercises from `Training charts.xlsx` ("Core training Exerc" sheet) to the Gym module's `Core` category in `EXERCISES` — Feet-up Crunches, Mountain Climber Plank, One-Arm Elbow & Side Plank, Sling Trainer "Marine Core", Windshield Wipers, Superman, Reverse Plank, Reverse Mountain Climber Plank, Side Hip Raises, Dumbbell Snatch, Sumo Deadlift (DB/KB), Barbell Deadlift, Barbell Squat. Skipped the chart's "Climbing specific core" rows (Roof Lever-ups, Steep Wall Cut & Catch, Steep Wall Traversing, Front Lever) — already exist under `CG-Core`. Skipped "Hanging knee lifts" — near-duplicate of existing "Hanging Knees". Compound lifts (snatch/deadlift/squat) kept in `Core` per Jo's choice rather than a separate category. Sets/reps/rest are estimated defaults (chart didn't specify per-exercise volume, except a Barbell Deadlift %1RM protocol) — Jo may want to tune these. Edited in both `index.html` (root) and `apps/training-hub/index.html` (kept in sync); deployed with the 2026-07-06 push.
- **Lattice Assessment rebuilt to match real protocol (2026-07-02):** the Climbing → Pyramid tab already had an empty "Lattice Assessment" section (`modal-assessment`, `climb_assessments` storage key, never used). Compared against Jo's actual Lattice Training PDFs (`My Lattice&Climbing training docs/Lattice Assess/`) and found it didn't match the real test protocol — rebuilt the modal + `saveAssessment()`/`renderAssessments()` (~line 1155, ~4640 in `index.html`). New fields: Height + Wingspan (→ auto Ape Index), Goal; **Finger Strength** (Two-Arm Max Hang — edge mm, fingerboard model, load ±kg, auto %BM score); **Power Endurance** (Strength Endurance 60% — load, time sec); **Pulling Strength** (2-Rep Max Pull-up — load ±kg, auto %BM score); **Hip Flexibility** (heel-to-heel cm, auto %height score); each of the 4 tests has a manual Priority selector (Low/Moderate/High, colour-coded badge — mirrors the coach's pink/yellow/green priority highlights in the PDF reports). Kept Recent Climbing Performance (Sport/Boulder OS+RP grades) in the form alongside the existing Pyramid grade tracking, per Jo's choice — the Lattice reports pair grades with test scores in one snapshot. Removed old mismatched fields (Side Split, One-Arm Hang + hang-detail text) since `climb_assessments` had zero saved records — no migration needed. **Historical data not yet logged:** Jo's PDFs contain real results from 14.08.23, 16.11.23, and a Mar 2024 coach report, but the scans are rotated/low-res with ambiguous OCR — numbers need Jo to confirm before entering, rather than risk mistranscription. Edited in both `index.html` and `apps/training-hub/index.html`; deployed 2026-07-02, first real entry logged and confirmed working by Jo same day.
- **Lattice Assessment: body composition fields + sign-display bug fix (2026-07-02):** added Body Fat (%), Skeletal Muscle (kg), Bone Mass (kg) to the assessment modal/save/render (smart-scale readings, shown in the entry's header line alongside body mass/height/ape index). Also fixed a display bug where a user-typed leading "+" on Finger Strength/Power Endurance/Pull-up load fields doubled up with the auto-added sign (e.g. "++2.5 kg") — new `_signed()` helper normalizes via `parseFloat` before re-adding the sign, so old saved entries render correctly too (no data migration needed, it's render-only).
- **Lattice Assessment moved into Self-Assess tab (2026-07-02):** per Jo's request, the whole "Lattice Assessment" card (practical tests) relocated from the Pyramid sub-tab to the top of the Self-Assess sub-tab, above the subjective 8-category wizard/summary — both assessment types (practical + subjective) now live in one place. `climbMode()` updated: `renderAssessments()` now fires on `mode==='selfassess'` instead of `'pyramid'`. `#assessment-list` DOM id unchanged, so no JS elsewhere needed updating.
- **Per-muscle body diagram (2026-07-06):** the Gym session body map (`drawBodyDiagram`) previously lit whole category zones — one Chest Press lit lats/traps too, and one Rehab calf exercise lit the entire leg. Now each exercise lights only the muscles it works: new `EX_MUSCLES` map (exercise name → muscle keys: pecs, delts, biceps, triceps, traps, lats, forearms, abs, obliques, lowerback, quads, glutes, hamstrings, calves), `MUSCLE_ZONE` (muscle → zone colour, unchanged palette), and `CAT_MUSCLES` (category → zone muscles — fallback for unmapped/custom exercises = old behaviour). `drawBodyDiagram` now takes the exercises array (still accepts a Set of cats for safety); both callers (volume summary + history detail) updated, `workedCats` removed. Keyed by exercise *name*, so old logged sessions work with no migration. Warm-up cardio + Stretching intentionally light nothing (as before). Muscle assignments are sensible defaults — Jo may want to tune some (e.g. "Hip Adductor" → quads shape as nearest proxy). "Fly Deltoid N2" confirmed by Jo as a reverse fly (2026-07-06) → delts+traps, same as Rear Deltoid N1. New exercise "Leg Press (Calf Raise)" added under Inferior Members (2026-07-06) → calves only — Jo uses the leg press both normally (quads+glutes) and toes-only for calf/Achilles work; previously only distinguishable via comments, which the muscle map can't read. (The Rehab "Leg Press (Calfs)" still exists for rehab-protocol logging.)
- **Climbing Gym tab rebuilt as fingerboard-first (2026-07-06):** Jo found the old tab unusable (generic gym-style logging, 40 CG- exercises, Build plan generator she never used) — her real sessions are fingerboard protocols tracked in a personal Excel (Set/Rep/Time/two rests/Kg/%/Day Max/BW/RPE). Rebuild: sub-tabs now **Log · History · Progress** (Build + FB templates removed). New session format `fbv:2` with `exercises:[{name,sets,reps,time,restRep,restSet,loads[],done[],rpe,notes}]`. `FB_PRESETS` seeded from her sheet (Hang 1-Arm Hang, Edge Iso Holds, AnaCap FB – Two Arms [repeaters 6×7×7"], MS Load Straight, 1/2 Wooden Ball, Wide/Narrow Wood Pinch, Small Finger Pinch Block — names match `WEIGHT_REF` where they exist); custom exercises persist in new `fb_custom_ex` key (added to `DATA_KEYS`). Log: inline picker (no modal), per-set load inputs (plain text keyboard so minus/assist loads typeable on iOS) + tap-done dots, editable sets/reps/time/both rests, fill-all input, RPE /10, notes; live chips show computed **Day Max**, **% of last Day Max**, **% BM** (from session body weight). "Repeat last session" button copies the latest fbv2 session (loads kept, dots reset). History (fbv2 only) shows per-exercise Day Max chips; detail modal has per-set loads with ✓, delete-exercise, "Load into Log". Progress: Day Max trend per exercise (kg + %BM, range-normalised bars so assisted negatives read) + Personal Bests list. **Fresh start:** pre-rebuild `fb_sessions` rows (no `fbv`) stay in storage/cloud/export and still count in Home/Week/Month aggregates (those read only `date`/`durationMins`), but are hidden from this tab. Old FB code removed: Build/plan generator, FB templates UI (DB methods + `fb_templates` key kept), shared-exercise-modal flow, old history/progress. Edited in both `index.html` and `apps/training-hub/index.html`.
- **Gym template rename + duration auto-name (2026-07-06):** each saved template row now has a ✏️ button → `renameTemplate(id)` (reuses `showTextModal`, same modal as generated-plan naming; syncs via `DB.set('gym_templates')`). New templates saved from a session are auto-named "areas · duration" (e.g. "Inferior + Superior + Core · 1h 34min") using `s.durationMins`; date suffix kept as fallback for sessions without times. Existing templates keep their old names — renameable by hand. Edited in both `index.html` and `apps/training-hub/index.html`; committed + deployed 2026-07-06.

- **Add Exercise modal is macro-phase aware (2026-07-29):** deload audit found that exercises added by hand in the Gym log ignored the macrocycle entirely — `#ex-intensity` was hardcoded `value="85"` and never re-seeded, so logging during a **deload** week suggested weights at 85% instead of 60% (~40% too heavy); sets/reps/rest also came from the `EXERCISES` library rather than the phase. Fix: new single-source-of-truth helpers `PHASE_INTENSITY = {hypertrophy:70, strength:85, power:90, deload:60}` + `currentGymPhaseId()` + `currentPhaseIntensity()` (null-safe: returns 85 when no macrocycle is configured, so behaviour is unchanged for users without one). `openAddExercise()` seeds the effort % from the active phase; `onExerciseSelect()` applies `phaseExDefaults()` for sets/reps. Rest priority: explicit session goal → macro phase → exercise library default. Also removed the two duplicate inline `_PHASE_INT` maps (in `gymMode('build')` and `loadTemplate`) that had drifted out of the same constant. Headless-tested: deload seeds 60% + 2×10 + 1'30", strength seeds 85%, session goal still wins for rest.
- **Custom exercises in the gym library (2026-07-29):** new `custom_exercises` localStorage key (added to `DATA_KEYS`, so it exports and cloud-syncs). `syncCustomExercises()` splices user entries into the `EXERCISES` array **in place** at `DOMContentLoaded` and after a cloud pull, so every existing lookup (`filterExercises`, `generatePlan`, `suggestWeight`, templates, body diagram `CAT_MUSCLES` fallback) picks them up with no other changes; entries carry `custom:true`. Entry point is inline: a `➕ Create new exercise…` option at the bottom of the Add Exercise picker opens `modal-new-exercise` (name, category, equipment, strength/cardio, sets/reps/rest, + distance/rate labels for cardio); on save it selects the new exercise straight into the session. Custom entries show `★` in the picker and a `🗑 Remove from library` strip when selected (removal does not touch already-logged sessions). **Scoped to gym categories only** — the Climbing Gym tab already has its own `addFBCustomExercise()` / `fb_custom_ex` flow from the 2026-07-06 fingerboard rebuild, and the CG- branch of the shared picker is dead code (`addExContext` is never set to `'fb'` any more). Headless-tested: add, persist across reload, delete, duplicate-name guard, blank-name guard, cardio variant, no CG leakage into the gym picker.
- **Weight suggestions no longer ratchet downward (2026-07-29):** found during the deload audit. `suggestWeightFull` estimated 1RM with Epley (`w × (1 + reps/30)`) from the latest logged session, but working sets are prescribed at a target %1RM and are *not taken to failure*, so Epley systematically under-reads them. Feeding that estimate back in each session multiplied it by `intensity × (1 + reps/30)`: hypertrophy `0.70 × 1.333 = ×0.933`, **deload `0.60 × 1.333 = ×0.800`**, strength `×0.992`, power `×0.990`. Simulating one full macrocycle from a true 100 kg 1RM (accepting each suggestion verbatim) drifted the estimate to **~20 kg**; deload was the worst offender and handed the depressed number to the next phase. Only a fresh assessment reset it. Root cause was a side effect of the 2026-07-17 "latest data wins" rewrite, which dropped the older `weight ÷ effort%` path — that path was drift-free. **Fix:** the session branch now derives `orm = w / (intensity/100)` when the logged entry carries a sane effort % (30–100; `addExerciseToSession` has always stored it), and falls back to Epley only when `intensity` is missing (older entries). Everything from 2026-07-17 is preserved: latest data still wins, newer assessment still beats an older session, and in-session weight changes still propagate immediately. Headless-tested: estimate holds at exactly 100 kg across a full hypertrophy→strength→power→deload cycle (was 100→20); logging 55 kg @70% drops the estimate to 78.6 and the next suggestion to 55 kg; logging 80 kg @70% raises it to 114.3/80 kg; a legacy entry with no intensity still uses Epley; a newer assessment still wins.
- **Deload phase — remaining known gaps (2026-07-29, declined for now):** (1) `generatePlan()` fills the full session duration, so deload's 2-set defaults just mean *more exercises* — total volume isn't actually reduced; would need a duration cap or max-exercise count for the deload phase. (2) Session Goal is blank during deload (`PHASE_GOAL.deload = ''`) — no Deload option in the dropdown and no `GOAL_HINTS` entry, so `_goalDefaultRest()` returns null and per-exercise rest falls back to the library default.

## Reference Folders
- `apps/garmin/V.1 POJ007_Garmin App files/` — V1 Garmin project files, decisions log, architecture docs
- `apps/training-hub/V1 training-hub files/` — V1 Training Hub project files, full decisions log, specs
